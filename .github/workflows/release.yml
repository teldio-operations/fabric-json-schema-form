on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  issues: write # For labels

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - id: release
        uses: googleapis/release-please-action@v4
        with:
          release-type: node
